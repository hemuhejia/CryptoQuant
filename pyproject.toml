[project]
name = "cryptoquant"
version = "0.1.0"
description = "Hyperliquid quant trading system skeleton"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
